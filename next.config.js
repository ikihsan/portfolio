/**
 * next.config.js
 * Minimal recommended Next.js config for this starter.
 */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    // App Router is stable in modern Next.js — keep opt-in flags minimal
  }
}

module.exports = nextConfig